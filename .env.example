# Environment Variables Configuration
# Copy this file to .env and fill in your values
# .env is git-ignored to keep your secrets safe

# LLM Provider Configuration
# Options: "ollama", "openrouter", "lm_studio", "openai"
# Default: "ollama" (free, local, recommended)
LLM_PROVIDER=openrouter

# OpenRouter API Key (required if LLM_PROVIDER=openrouter)
# Get your free API key from: https://openrouter.ai/
# Leave empty if not using OpenRouter
OPENROUTER_API_KEY=

# OpenAI API Key (required if LLM_PROVIDER=openai)
# Get your API key from: https://platform.openai.com/api-keys
# Leave empty if not using OpenAI
OPENAI_API_KEY=
